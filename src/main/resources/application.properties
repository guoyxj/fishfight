# 配置服务器端口，默认是8080
server.port=8080
server.servlet.session.timeout=30s
# 这个是配置模板路径的，默认就是templates，可不用配置
spring.thymeleaf.prefix=classpath:/templates/
# 这个开发配置为false，避免改了模板还要重启服务器
spring.thymeleaf.cache=false
spring.devtools.restart.enabled=true
# 下面3个不做解释了，可以不配置
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8
# 模板的模式
spring.thymeleaf.mode=HTML5

#配置数据源
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
spring.datasource.url=jdbc:mysql://49.233.131.146:3306/fishdb?useUnicode=true&characterEncoding=utf8
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=guoyxpico

#JPA
jpa.properties.hibernate.hbm2ddl.auto=create
jpa.properties.hibernate.hbm2ddl.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
jpa.properties.hibernate.hbm2ddl.format_sql=true
jpa.properties.hibernate.hbm2ddl.show-sql=true